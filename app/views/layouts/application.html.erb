<!DOCTYPE html>
<html>
  <head>
    <title>BookLibrary</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/semantic-ui/1.11.8/semantic.min.css"/>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
      <div class="ui secondary menu">
          <%= link_to "Home",root_path, class: "active item"%>
          <%=link_to "Books",books_path, class: "item" %>
          <%= link_to "Libraries", libraries_path, class: "item" %>

          <div class="right menu">
            <% if logged_in? %>
              <%=link_to logged_in_user.first_name + ' is logged in!' + ' Go to profile',logged_in_user, class: "ui item"%>
              <%= link_to "Logout", logout_path,method: :post, class: "ui item"%>
            <% else %>
              <%=link_to "Login", login_path, class: "ui item"%>
            <% end %>
          </div>
    </div>
    <%= yield %>
  </body>
</html>
